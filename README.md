# LAB-Django-69
สร้าง venv
python3 -m venv venv

เข้า venv (Mac)
source venv/bin/activate

จากนั้นติดตั้ง Django

pip install django

และติดตั้ง Pillow เพราะ Product ใช้รูปภาพ ImageField ตาม Lab

pip install pillow

รัน Server

python3 manage.py runserver